<!DOCTYPE html>
<html>
  <head>
    <title>Gameranking</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="mb-5">
      <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #fa8072">
        <div class="container">
          <a class="navbar-brand" href="/"><span class="logo">logo</span></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if admin_signed_in? %>
              <% if request.fullpath.include? "/admin" %>
                <li class="nav-item">
                  <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: 'nav-link text-light btn btn-outline-light ml-3' %>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to "概要", about_path, class: 'nav-link text-light btn btn-outline-light' %>
                </li>
              <% end %>
            <% elsif  user_signed_in? %>
              <div class="d-flex align-items-center text-white">ようこそ、<%= current_user.nickname %>さん!</div>

              <li class="nav-item">
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'nav-link text-light btn btn-outline-light ml-3' %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "概要", about_path, class: 'nav-link text-light btn btn-outline-light' %>
              </li>
              <li class="nav-item">
                <%= link_to "新規登録", new_user_registration_path, class: 'nav-link text-light btn btn-outline-light ml-3' %>
              </li>
              <li class="nav-item">
                <%= link_to "ログイン", new_user_session_path, class: 'nav-link text-light btn btn-outline-light ml-3' %>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
  </body>
</html>

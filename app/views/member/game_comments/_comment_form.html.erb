<%= form_with model:[game, game_comment], local: false do |f| %>
  <div class= "form-group">
    <%= f.text_area :game_comment, class: 'form-control game_comment_comment' %>
  </div>
  <div class= "form-group">
    <%= f.submit "送信" %>
  </div>
<% end %>